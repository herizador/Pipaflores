services:
  - type: web
    name: pipaflores
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    rootDir: .
    envVars:
      - key: NODE_VERSION
        value: 18

